import './Tips.css'

function Tips() {
  const tips = [
    "Пийте склянку води відразу після пробудження.",
    "Додайте лимон або м'яту для смаку.",
    "Тримайте багаторазову пляшку завжди під рукою."
  ];

  return (
    <div className="tips-container">
      <h3>💡 Поради</h3>
      <ul>
        {tips.map((tip, index) => <li key={index}>{tip}</li>)}
      </ul>
    </div>
  );
}

export default Tips;